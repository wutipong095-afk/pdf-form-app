# งานค้าง (Backlog)

อัปเดตล่าสุด: 2026-08-11  
ใช้คู่กับ [ROADMAP.md](../ROADMAP.md) — เอกสารนี้รวบจุดที่ยังไม่ทำ / ทำค้างจากคุยและรีวิว

สถานะผลิตภัณฑ์โดยประมาณ: **CP0–CP4 ผ่าน** · แจ้งอัปเดต merge แล้ว · hardening 0.1.9

---

## A. ต้องปิดก่อนส่งโรงเรียนรอบใหญ่

| # | งาน | หมายเหตุ |
|---|-----|----------|
| A1 | Merge / ปล่อยฟีเจอร์แจ้งอัปเดต | ทำแล้ว (PR #10) — บิวด์คู่กับ 0.1.9 + hardening |
| A2 | ตั้งโฮสต์อัปเดตบน VPS | วาง `/releases/latest.json` + Setup.exe · HTTPS · ไฟล์ `update_feed.url` ในชุดติดตั้งหรือคู่มือ |
| A3 | แพ็กฟอร์มทดลอง (ฟรี) | ใบลา / ขออนุญาตไปราชการ ฯลฯ — allowlist ไฟล์ทางการ + กันปลอมชื่อ (คล้าย demo hash) |
| A4 | ล็อกโหมดฟรี | ไม่มีไลเซนต์: ใช้ได้แค่ฟอร์มในแพ็กทดลอง · **ห้ามอัปโหลด PDF ภายนอก** (หรืออัปโหลดแล้วสร้าง PDF ไม่ได้ — แนะนำห้ามอัปโหลดเลย) |
| A5 | คู่มือภาษาไทยสั้น | ติดตั้ง → ใส่คีย์/ใช้ฟรี → กรอก → สำรอง · วิดีโอสั้น (CP7) |
| A6 | นโยบายซัพพอร์ตชัด | ดูแลกี่ปี · ย้ายเครื่องปีละกี่ครั้ง · ช่องทางออกคีย์ |
| A7 | Pilot ≥ 2 โรงเรียน | CP6 — ใช้จริง 1 สัปดาห์ + เก็บ log/feedback |

---

## B. UX / ผลิตภัณฑ์ในแอป

| # | งาน | หมายเหตุ |
|---|-----|----------|
| B1 | การ์ดโปรโมทมุมล่างขวา / ท้ายไซด์บาร์ | ไม่ใช่ modal · ปิดได้ · จำใน localStorage · **ห้ามแทรกใน PDF ที่สร้าง** · ลิงก์ไปตารางสอน / ข้อสอบ / วัดผล / ดนตรี–กีฬา |
| B2 | ข้อความโหมดฟรีชัดใน UI | เช่น “ทดลองใช้ฟอร์มตัวอย่าง — โหลดฟอร์มโรงเรียนได้เมื่อมีไลเซนต์” |
| B3 | (ทางเลือก) เลือกฟอนต์ใน UI | นอกเหนือ `FONT_PATH` — ลดซัพพอร์ตตอนขยายภาษา/ต่างประเทศ |
| B4 | คู่มือ Word → PDF → มาร์ค | สำหรับผู้ใช้ที่สร้างฟอร์มเอง |

---

## C. ธุรกิจ / ราคา (ตัดสินใจแล้วบางส่วน — ยังไม่ลงมือในโค้ด)

| # | งาน | สถานะแนวคิด |
|---|-----|-------------|
| C1 | โมเดลหลัก: ฟรีแพ็กตัวอย่าง + ขายคีย์เมื่อโหลด PDF เอง | ตกลงแนวแล้ว — รอ implement A3–A4 |
| C2 | ราคาเปิดตัวแนะนำ ~490 บาท/เครื่อง (ดูแล ~5 ปี) | ยังไม่ต้องใส่ในแอปก็ได้ — ใส่ในคู่มือ/เพจขาย |
| C3 | **ไม่ทำ** เติมเงินรายพิมพ์ (1 บาท/ครั้ง) ช่วงแรก | เลื่อนไปยาว ๆ — ขัดออฟไลน์ + ต้นทุนระบบ |
| C4 | รายได้เสริม: โฆษณาแฝงสินค้า/โปรแกรมอื่น | ทำผ่าน B1 — ไม่บังคับดูโฆษณา |
| C5 | ต่างประเทศ / นักพัฒนา / ใบเสร็จ | UI TH/EN + docs อังกฤษทำแล้ว (`locales/`, README.en.md) — ใบเสร็จ/ราคาต่างประเทศยังไม่เร่ง |

---

## D. อัปเดตอัตโนมัติ (ระดับถัดไป)

ตอนนี้ทำแล้ว: **แจ้งมีอัปเดต + ลิงก์ดาวน์โหลด Setup**

| # | งาน | หมายเหตุ |
|---|-----|----------|
| D1 | อัปโหลด Setup + `latest.json` บน VPS จริง | ดีกว่า Drive; GitHub Releases เป็นทางเลือก |
| D2 | ใส่ `update_feed.url` ในชุดที่ส่งลูกค้า | หรือสอนวางเองหลังติดตั้ง |
| D3 | (ยังไม่ทำ) ดาวน์โหลดแล้วเปิด Setup ให้เอง | ขั้น 2 หลัง D1 นิ่ง |
| D4 | (ยังไม่ทำ) อัปเดตเงียบทั้งก้อน | ไม่เร่ง — เสี่ยงกับโรงเรียนออฟไลน์ |

---

## E. เทคนิค / คุณภาพจากรีวิว (ทำแล้วบางส่วน)

| # | งาน | สถานะ |
|---|-----|--------|
| E1 | Seed demo เฉพาะค่าแนะนำ | ทำแล้วใน 0.1.8 |
| E2 | Browse โฟลเดอร์แบบ async | ทำแล้วใน 0.1.8 |
| E3 | ข้อความ installer เรื่องรีสตาร์ท | ทำแล้วใน 0.1.8 |
| E4 | ล้างกิ่ง local ที่ merge แล้ว | ยังไม่ได้ทำบนเครื่องพัฒนา (optional) |
| E5 | CSRF / headers / PDF+ZIP limits / admin gates | ทำแล้วใน 0.1.9 |
| E6 | Dockerfile คัดลอก `update_core.py` + เอกสาร Docker ขั้นสูง | ทำแล้วใน 0.1.9 |
| E7 | Docker multi-user: แยกคลัง / XFF / login eviction | เลื่อน — ไม่ขาย server mode ตอนนี้ |

---

## F. เช็กพอยต์ ROADMAP ที่ยังเปิด

- [ ] **CP5** Form pack โรงเรียน 3–5 แบบพร้อมมาร์ค (เชื่อม A3)
- [ ] **CP6** Pilot โรงเรียนจริง ≥ 2 แห่ง
- [ ] **CP7** พร้อมขาย: คู่มือ · ออกคีย์ · วิดีโอ · นโยบาย 5 ปี

---

## ลำดับแนะนำทำต่อ

1. บิวด์/ปล่อย Setup 0.1.9 (hardening)  
2. ตั้ง VPS `/releases/` + ทดสอบจากเน็ตภายนอก  
3. แพ็กฟอร์มทดลอง + ล็อกอัปโหลดตอนไม่มีไลเซนต์  
4. การ์ดโปรโมทสินค้าอื่น (ไม่กวนงาน)  
5. คู่มือไทย + pilot 2 โรงเรียน  

---

## อ้างอิง

- แผนใหญ่: [ROADMAP.md](../ROADMAP.md)
- อัปเดต: [UPDATE.md](UPDATE.md)
- ติดตั้ง: [INSTALLER.md](INSTALLER.md)
- สำรอง: [BACKUP.md](BACKUP.md)
