# แผนขายและไลเซนต์ FormDD

อัปเดต: 2026-08-21  
ใช้คู่กับ [BACKLOG.md](BACKLOG.md) และ [ROADMAP.md](../ROADMAP.md)

เอกสารนี้ล็อกทิศทางราคาไทยแบบเลือกอายุคีย์ + ราคาต่างประเทศ + โมเดลทดลองชุดเดียว  
**ยังไม่ลงมือในโค้ดครบ** — ใช้เป็นแผนแก้ทีละเฟส

---

## เป้าหมาย

ผู้ใช้ไทยและต่างประเทศใช้ **ตัวติดตั้งชุดเดียวกัน**

```text
ดาวน์โหลด Setup → ติดตั้ง → ทดลองฟอร์มตัวอย่าง
    → ซื้อรายเครื่อง → Activate → ใช้ PDF ของตัวเอง
ไม่ต้องติดตั้งใหม่ ไม่มีตัว Trial / Full แยกไฟล์
```

ต่างกันแค่ราคา สกุลเงิน และช่องทางจ่ายเงิน

---

## การตัดสินใจที่ล็อกแล้ว

| หัวข้อ | คำตัดสิน |
|--------|----------|
| ไฟล์โปรแกรม | Setup ชุดเดียวทั้งโลก ไม่แยก trial.exe / full.exe |
| โหมดทดลอง | ใช้ได้เฉพาะฟอร์มตัวอย่างทางการ (ตรวจ hash) |
| กรอกด่วน / Quick Fill | ฟรีบนฟอร์มตัวอย่างชุดเดียวกับโหมดทดลอง · PDF ของผู้ใช้ต้องมีคีย์ · ไม่แยกขายเป็นแพ็ก · ไม่วอเตอร์มาร์ก / โควตาใบฟรี |
| หลัง Activate | โปรแกรมเดิมปลดล็อก ไม่โหลดชุดใหม่ — รวมเทมเพลตและกรอกด่วน |
| หน่วยขาย | รายเครื่อง (1 คีย์ = 1 `machine_id`) — ไม่ใช่ต่ออีเมล |
| บัญชีสมาชิก | อีเมล = บัญชี ถือได้หลายที่นั่ง อยากได้ 2 เครื่องก็ซื้อ 2 ที่นั่ง |
| แพ็กชื่อ Team / School | **ไม่ทำส่วนลดแพ็ก** — หน้าอังกฤษราคาเดียว US$19 / เครื่อง คูณจำนวน ไม่มีฟีเจอร์แยก |
| Site license / floating | **ไม่ทำ** ในระยะนี้ |
| สมัครสมาชิกรายเดือน | **ไม่ทำ** — prepaid ตามอายุในคีย์ ไม่ต่ออัตโนมัติ |
| เติมเงินรายพิมพ์ | **ไม่ทำ** (C3) |
| ออกคีย์ | คีย์ผูก `machine_id` 16 hex ตาม `license_core.py` |
| ตรวจออนไลน์ตอนเปิดแอป | **ไม่บังคับ** — activate ครั้งเดียว แล้วออฟไลน์ได้ |

### ราคา (ยังไม่ใส่ในแอป — ใส่เพจขาย / คู่มือ)

**ไทย (ช่องทางท้องถิ่น) — เลือกอายุคีย์ต่อเครื่อง**

ราคาเป็น prepaid ก้อนเดียว ไม่คิดรายเดือน ไม่ต่ออัตโนมัติ  
แอปชุดเดียว อายุอยู่ในคีย์ที่ออกให้ (`--term`)

| แผน | ราคา / เครื่อง | ออกคีย์ | `--days` |
|---|---|---|---|
| 1 ปี | **99 บาท** | `--term 1` | 365 |
| ซื้อขาด (LT) | **299 บาท** | `--days 36500` | ใช้เวอร์ชันนั้นได้ตลอดไป · แก้บั๊ก/ความปลอดภัยของเวอร์ชันนั้น · ไม่รวม major ใหม่ |
| ซื้อขาด + อัปเดต 3 ปี | **499 บาท** | `--days 36500` | สิทธิ์ LT + รับเวอร์ชันใหญ่ใหม่ 3 ปีหลังซื้อ |

- หน่วยยังเป็น **1 ที่นั่ง = 1 เครื่อง** — ซื้อกี่เครื่องคูณแถวนั้น
- โรงเรียน/ใบเสนอราคา **แนะนำซื้อขาด + อัปเดต 3 ปี (499 บาท)** ถ้าลูกค้าไม่ระบุแผน
- แถว 1 ปีสำหรับทดลองจ่ายเงินจริง ไม่แถมเครื่องที่สอง
- อย่าโชว์เทียบรายเดือนบนหน้าขาย
- แถว 3 / 5 / 10 ปีเลิกขายแล้ว — ย้ายเครื่องคีย์เก่าด้วย `--days N` ตามอายุที่เหลือเท่านั้น ไม่ใช้ `--term` เริ่มอายุใหม่
- ซื้อขาด (LT) = ใช้โปรแกรมเวอร์ชันที่ซื้อได้ตลอดไป รวมแก้บั๊กและอัปเดตความปลอดภัยของเวอร์ชันนั้น **ไม่รวม major version ใหม่**
- ซื้อขาด + อัปเดต 3 ปี = สิทธิ์ LT และรับเวอร์ชันใหญ่ใหม่ภายใน 3 ปีหลังซื้อ
- ซื้อขาดใช้ `--days 36500` (คีย์มีวันหมดอายุวันเดียว จึงใช้ ~100 ปีแทน perpetual) · `--days` สั้นกว่านั้นไว้ย้ายเครื่องที่เหลือวัน
- ตัวอัปเดตในแอปยังชุดเดียวทั้งโลก จึงยังตัด major ตามแผนในโปรแกรมไม่ได้ในตอนนี้ — ข้อตกลง LT / major 3 ปีเป็นนโยบายผู้ขายจนกว่าจะแยกฟีดได้

**ต่างประเทศ (หน้าอังกฤษ — จ่ายครั้งเดียว ไม่ขายว่าถูกกว่า Adobe)**

จุดขายคือเอา PDF แบบฟอร์มที่มีอยู่แล้ว → ปักตำแหน่งครั้งเดียว → กรอกซ้ำได้เรื่อย ๆ → ข้อมูลอยู่ในเครื่อง  
อย่าเทียบฟีเจอร์กับ Acrobat / Foxit และ**ห้ามบอกหรือเทียบราคาบาท**

| แพ็กเกจ | ราคาแนะนำ | ความหมาย |
|---|---|---|
| Free Trial | US$0 | ทดลองแอปจริงกับฟอร์มตัวอย่าง |
| 1 เครื่อง | **US$19** | ราคาหลัก · 10 ปี · จ่ายครั้งเดียว |
| หลายเครื่อง | US$19 × N | คูณจำนวนเครื่อง · ไม่มีส่วนลดอัตโนมัติ · อีเมลจำนวนมา |

ข้อความราคาหลัก:

> US$19 for a 10-year license — no auto-renewal

- ระบุ “$19 for 10 years” ให้เห็นก่อนซื้อ: 3,650 วันนับจากออกคีย์ ไม่ใช่ lifetime
- รวมเวียดนาม อินโดนีเซีย ฟิลิปปินส์ และตลาดอังกฤษอื่นในหน้าราคาเดียว ห้ามแยกเพจถูกกว่า
- หน้าเว็บสัญญาแค่ **อัปเดตตราบที่คีย์ยังใช้ได้** (วันหมดอายุบนแถบไลเซนต์) เพราะ `latest.json` ยังชุดเดียวทั้งโลก ตัดฟีเจอร์ที่ปีที่ 5 ไม่ได้
- เป้าในอนาคต: แยกสิทธิ์อัปเดต 5 ปี เมื่อฟีดอัปเดตแยกได้ — อย่าโฆษณาเส้นตัดนี้จนกว่าจะทำได้
- แอปชุดเดียวทุกที่นั่ง — ซื้อที่นั่ง ไม่ซื้อฟีเจอร์พิเศษ
- คีย์ยังผูก 1 เครื่อง; หลายเครื่องคือหลายที่นั่งในออเดอร์เดียวกัน ไม่ลดต่อเครื่อง

ช่วงนี้คีย์ต่างประเทศออก `--term 10` — หน้าเว็บชี้ไปที่วันหมดอายุในแอป ระบุอายุ 10 ปี (3,650 วันนับจากออกคีย์) ก่อนซื้อ และไม่สัญญาตัดอัปเดตที่ 5 ปี

**ราคาต่อเครื่องสำหรับการซื้อหลายเครื่อง**

คิดราคาต่อเครื่องของแผนที่เลือกคูณจำนวนเครื่อง ไม่มีส่วนลดอัตโนมัติ
หนึ่งสิทธิ์ใช้ได้หนึ่งเครื่อง การซื้อหลายเครื่องต้องออกคีย์ตามรหัสของแต่ละเครื่อง

ตัวอย่าง: ซื้อขาด+อัปเดต 3 ปี 499 บาท × 5 เครื่อง = 2,495 บาท

โรงเรียน/องค์กรไทยใช้ตารางเดียวกัน ไม่มีแพ็กที่แพงกว่าบุคคล

**บัญชีกับที่นั่ง**

- อีเมลเป็นบัญชีสำหรับติดต่อ ดูออเดอร์ ขอย้ายเครื่อง
- ที่นั่งคือไลเซนต์หนึ่งใบ ผูกหนึ่ง `machine_id`
- โรงเรียนใช้อีเมลพัสดุอีเมลเดียวซื้อหลายที่นั่งได้
- ย้ายเครื่อง = ปลดที่นั่งเดิม ผูกเครื่องใหม่ (โควตา 2 ครั้งต่อใบต่อปี) ไม่ใช่ได้เครื่องที่สองใช้พร้อมกัน

**อายุคีย์ในไฟล์ไลเซนต์**

รูปแบบคีย์มีวันหมดอายุวันเดียว (`PFM2.{mid}.{YYYYMMDD}.{sig}`)

- ไทย: ออกสิทธิ์ใหม่ 1 ปี `--term 1` · ซื้อขาด `--days 36500` · ย้ายคีย์เก่าทุกแผนใช้ `--days N` ตามอายุที่เหลือ คงสิทธิ์อัปเดตเดิม และเลิกใช้เครื่องเก่า
- ต่างประเทศ: `--term 10` (ภายใน — หน้าเว็บระบุอายุ 10 ปี และไม่สัญญาตัดอัปเดตที่ 5 ปี; อัปเดตตราบที่คีย์ยังใช้ได้)

แอปชุดเดียว อายุอยู่ในคีย์ที่ออกให้ — อย่าใส่ราคาลงในตัวแอป

**ซัพพอร์ต / ย้ายเครื่อง** (A6 — ต้องเขียนบนเพจขาย)

- ย้ายเครื่องเมื่อเครื่องเก่าเลิกใช้
- โควตารอบแรก: **2 ครั้งต่อใบต่อปี**
- เฟสแรกทำมือ (ออกคีย์ใหม่ให้รหัสเครื่องใหม่)
- สำรองข้อมูลไม่รวม `machine_id` / `license.json` — คงตาม [BACKUP.md](BACKUP.md)

**ข้อความขายต่างประเทศ (แนะนำ)**

> Your existing PDF forms. Mark once, fill again. $19 for 10 years — no auto-renewal.

อย่าเทียบฟีเจอร์กับ Acrobat / Foxit  
อย่าโชว์ราคาต่อปี  
อย่าเขียนว่ามีราคาถูกกว่าที่ไทย

---

## สถานะตอนนี้ vs เป้า

| จุด | ตอนนี้ | เป้า |
|-----|--------|------|
| ตัวติดตั้ง | ชุดเดียว ปลดล็อกด้วยคีย์ | คงไว้ |
| ทดลอง | สร้าง PDF ได้แค่ `demo-form.pdf` (hash) | **เฟส 1 ทำแล้ว** — 3 ใบ + hash |
| อัปโหลดไม่มีคีย์ | อัปโหลดได้ แต่สร้าง PDF ไม่ได้ (402) | **เฟส 1 ทำแล้ว** — ห้ามอัปโหลด (402) |
| คลังเอกสารไม่มีคีย์ | ยังเลือกโฟลเดอร์นอกได้ | **เฟส 1 ทำแล้ว** — ล็อกจนกว่าจะมีคีย์ |
| ออกคีย์ | ผู้ขายรัน `scripts/gen_license.py <รหัส> --term 1` หรือ `--days 36500` | **เฟส 2** อีเมลมือ + เทมเพลต · หน้า Activate เป็นเฟส 3 |
| เว็บ | สอนใช้ + ทดลองในเบราว์เซอร์ | **เฟส 2 ทำแล้ว** — ดาวน์โหลด + ราคาไทย/USD คนละหน้า |
| ราคาในเอกสารโปรเจกต์ | ไทย 99 / 299 / 499 | ตารางต่างประเทศในแผนนี้ + หน้า `pricing.en.html` |

`formpacks/v1/` เป็นเทมเพลตจุดสำหรับฟอร์มโรงเรียน **ไม่ใช่** แพ็กทดลอง — อย่าผสมกับ allowlist โหมดฟรี

---

## สิ่งที่ห้ามทำ

- แยกไบนารี Trial / Full
- ให้ลูกค้าต่างประเทศส่งรหัสเครื่องมาทางอีเมลเป็นช่องทางหลักระยะยาว
- ใส่ `keys/ed25519_private.pem` หรือ `gen_license.py` ใน Setup
- บังคับออนไลน์ทุกครั้งที่เปิดโปรแกรม
- Site license, floating license, คิดเงินรายพิมพ์
- ราคาต่างประเทศต่ำจนค่าธรรมเนียม + ออกคีย์กินมาร์จิ้น (`$5–12` เป็นหลัก)
- เปิดกรอกด่วน / Quick Fill ฟรีบน PDF ของผู้ใช้ หรือแยกขายเป็นสินค้าคนละชิ้น

---

## เฟสงาน

ทำตามลำดับ อย่าข้ามเฟส 1 ถ้าจะให้คนดาวน์โหลดเองแล้วเข้าใจโหมดทดลอง

### เฟส 1 — ล็อกโหมดทดลองในแอป (ต้องทำก่อนขาย)

ปิดช่องที่คนเข้าใจว่าโปรแกรมพัง หรือใช้ของจริงโดยไม่จ่าย

**1.1 แพ็กฟอร์มทดลอง (A3 / CP5 บางส่วน)**

- ขยาย allowlist จากชื่อเดียว (`demo-form.pdf`) เป็นชุดไฟล์ทางการ + hash
- ตัวอย่างไทย: ใบลา / ใบเบิก ตามที่มีบนเว็บแล้วได้
- ตัวอย่างสากล: อย่างน้อย 1 ใบภาษาอังกฤษ (leave / request)
- ตรวจแบบเดียวกับ `is_canonical_demo_pdf()` — กันเปลี่ยนชื่อไฟล์แล้วใช้เอกสารอื่น
- จุดแก้หลัก: `license_core.py` (`can_fill_document`), โฟลเดอร์ `demo/`, seed ตอนเปิดครั้งแรก

**1.2 ล็อกโหมดฟรี (A4)**

ไม่มีไลเซนต์แล้ว:

- `POST /api/upload` คืน 402 + ข้อความชัด
- เปิดเอกสารจากคลังที่ไม่ใช่ไฟล์ในแพ็กทดลอง ไม่ได้
- UI ซ่อน/ปิดปุ่มอัปโหลดและปุ่มคลัง (B2)

จุดแก้หลัก: `app.py`, `frontend/src/docs.ts`, `frontend/src/license.ts`, `locales/th.json`, `locales/en.json`

**1.3 ข้อความทดลองชัด (B2)**

- แถบไลเซนต์บอกว่าทดลองได้เฉพาะฟอร์มตัวอย่าง
- ปุ่มคัดลอกรหัสเครื่อง (ลดส่งรหัสผิด)
- ลิงก์ไปหน้าซื้อ / Activate (ใส่ได้หลังมี URL จริง)

**ผ่านเมื่อ**

- ไม่มีคีย์: ใช้ฟอร์มตัวอย่างได้ครบ และอัปโหลด PDF นอกไม่ได้
- มีคีย์: อัปโหลดและคลังใช้ได้ตามเดิม
- เปลี่ยนเนื้อไฟล์ตัวอย่างแล้วสร้าง PDF ไม่ได้

### เฟส 2 — นโยบาย + หน้าเว็บดาวน์โหลด (ทำแล้วใน `website/` + docs/SUPPORT.md)

ยังไม่ต้องมีร้านอัตโนมัติ ใช้ได้กับโรงเรียนไทยและออเดอร์ต่างประเทศจำนวนน้อย

**2.1 นโยบายซัพพอร์ต (A6)**

เขียนสั้นในคู่มือ/เพจ:

- อายุคีย์ไทยตามแผนที่ซื้อ (1 ปี หรือซื้อขาด) / ต่างประเทศ: จ่ายครั้งเดียว อัปเดตและใช้ได้ถึงวันหมดอายุบนแถบไลเซนต์
- ย้ายเครื่อง 2 ครั้ง/ปี
- ช่องทางออกคีย์ (อีเมล ชั่วคราว → หน้า Activate เฟส 4)

**2.2 เว็บ `website/`**

- หน้าแรก: ปุ่มดาวน์โหลด Setup (ชุดเดียว) + ทดลองบนเว็บที่มีอยู่
- หน้า Pricing: ตารางรายเครื่อง + ราคารวมตามจำนวนเครื่อง + ราคาภูมิภาค
- หน้า Pricing อังกฤษ: US$19 / เครื่อง / 10 ปี คูณจำนวน ไม่ปนตารางบาท ไม่มีแพ็ก School/Org ถูกกว่า
- คู่มือสั้น: ติดตั้ง → ทดลอง → คัดลอกรหัสเครื่อง → ได้คีย์ → วางในแอป

**2.3 ออกคีย์มือ (ช่องทางชั่วคราว)**

คง `scripts/gen_license.py`  
อีเมลตอบรับต้องมีเทมเพลต: ขอรหัส 16 ตัว, ส่งคีย์กลับ, สอนวางในแถบไลเซนต์  
บนเว็บอย่าสัญญา instant license จนกว่าเฟส 4 จะเสร็จ

**ผ่านเมื่อ**

- คนโหลด Setup จากเว็บแล้วทดลองได้โดยไม่ถามว่าตัวไหนคือตัวจริง
- ผู้ขายออกคีย์จากรหัสเครื่องได้ทั้งไทย (`--term` ตามแถวที่จ่าย) และต่างประเทศ (`--term 10` โดยระบุอายุ 10 ปีให้ชัดก่อนซื้อ)

### เฟส 3 — หน้า Activate ให้ลูกค้าผูกเครื่องเอง

เป้าขั้นต่ำก่อนขายต่างประเทศเป็นช่องทางหลัก

```text
จ่ายเงิน (หรือได้สิทธิ์จากผู้ขาย)
    → ลิงก์ Activate
        → วาง Machine ID
            → ได้คีย์ทันที
                → วางในแอป ใช้ออฟไลน์ต่อ
```

กติกา:

- จ่าย/ออกสิทธิ์ได้ก่อนติดตั้ง ไม่ต้องมีรหัสเครื่องตอนcheckout
- 1 ออเดอร์ = N ที่นั่ง ผูกทีละเครื่องจนครบ
- Private key อยู่เฉพาะเซิร์ฟเวอร์ผู้ขาย ไม่ลง Setup
- ตรวจออนไลน์เฉพาะตอนออกคีย์
- มี log: ออเดอร์, machine_id, วันหมดอายุ — **ห้าม log คีย์เต็ม**
- ย้ายเครื่อง: เพจ “Replace this computer” นับโควตา ไม่ให้อีเมลหาผู้ขายทุกครั้งระยะยาว

อย่ารวมระบบนีเข้าแอปลูกค้าแบบให้แอปถือ private key

**ผ่านเมื่อ**

- ลูกค้าไม่ต้องส่งรหัสเครื่องมาทางอีเมลเป็นปกติ
- ซื้อ 10 เครื่องแล้ว activate ทีละเครื่องได้จนครบโควตา

### เฟส 4 — ชำระเงิน (หลังเฟส 3)

- ไทย: โอน/พร้อมเพย์ ตามช่องทางที่มี + ออกสิทธิ์ในระบบ Activate
- ต่างประเทศ: Paddle / Lemon Squeezy / Stripe แล้วโยง webhook เข้าโควตาที่นั่ง
- Checkout รอบแรกขาย **1 ที่นั่ง 1 แผน** ให้จบ (ไทยให้เลือก 1 ปี / ซื้อขาด / ซื้อขาด+อัปเดต 3 ปี)
- หลายเครื่อง: ปุ่มจำนวน หรือฟอร์ม “Buying for a team?” จนกว่าร้านปริมาณจะคุ้มสร้าง
- บัญชีอีเมลถือหลายที่นั่งได้ — ไม่ล็อก 1 อีเมล = 1 เครื่อง และไม่แถมเครื่องที่สองในราคาใบเดียว

อย่ารอร้านปริมาณครบก่อนเปิดขายบุคคล

---

## ลำดับทำจริง (ตัดงาน)

1. เฟส 1.1–1.3 ในแอป (A3, A4, B2) — คนโหลดแล้วไม่สับสน  
2. เฟส 2 เว็บดาวน์โหลด + หน้า Pricing ตามตารางนี้  
3. เทมเพลตอีเมลออกคีย์มือ + นโยบาย A6  
4. เฟส 3 หน้า Activate  
5. เฟส 4 ชำระเงินอัตโนมัติ  

งานที่เลื่อนได้: การ์ดโปรโมท B1, เลือกฟอนต์ B3, อัปเดตเงียบ D4, site license

---

## ไฟล์ที่คาดว่าจะแตะ

| เฟส | ไฟล์ |
|-----|------|
| 1 | `license_core.py`, `app.py`, `frontend/src/docs.ts`, `frontend/src/license.ts`, `locales/*.json`, `demo/` |
| 2 | `website/index.html`, หน้า pricing ใหม่, `docs/INSTALLER.md`, `docs/INSTALLER.en.md` |
| 3 | บริการออกคีย์ใหม่บน VPS (แยกจาก Setup ลูกค้า), ไม่ใส่ private key ใน repo สาธารณะ |
| 4 | checkout / webhook — ยังไม่ผูก vendor จนกว่าจะเลือกตัวจ่ายเงิน |

อย่าใส่ราคาลงในตัวแอปเป็นค่าคงที่ที่ต้องออก Setup ใหม่ทุกครั้งที่เปลี่ยนราคา

---

## โฮสต์เว็บ / อัปเดต / คีย์

เว็บและตัวติดตั้งอยู่ที่ **formdd.xambrain.com** (โฟลเดอร์ `/var/www/formdd`) คู่กับ Shed บน VPS เดียวกันผ่าน Caddy ไซต์แยก

- ไฟล์ที่ลูกค้าโหลด: `FormDD-Setup-0.3.0.exe` (ไม่ทับรุ่นเก่า) · `latest.json` ชี้รุ่นใหม่ได้อย่างเดียว
- GitHub Release สร้าง**ทุกครั้ง**เป็นสำเนา (`FormDD-Setup-x.y.z.exe` + `SHA256SUMS.txt`) — ไม่ใช่โฮสต์หลัก
- `/var/www/formdd` มีได้แค่ HTML/CSS/JS/installer/`latest.json`  
  **ห้าม**วาง private key, `gen_license.py`, `.env`, ฐานข้อมูลลูกค้า
- เฟส 3 Activate ออนไลน์: แยก `api.formdd.xambrain.com` (หรือ `/api` ผ่าน reverse proxy) — คีย์อยู่ใน secret ของ backend เท่านั้น

รายละเอียด: [UPDATE.md](UPDATE.md) · ตัวอย่าง Caddy: `website/Caddyfile.formdd`

---

## ความเสี่ยงที่ต้องรับ

- `machine_id` เป็นไฟล์ UUID ใน AppData ไม่ใช่ซีเรียลฮาร์ดแวร์ — คนคัดลอกโฟลเดอร์ data ได้ เฟสแรกใช้กติกา + ย้ายเครื่องแบบมีโควตา ไม่ทำ TPM ในรอบนี้
- เฟส 3 ทำให้ private key อยู่บน VPS — ต้องล็อกให้เรียกออกคีย์ได้เฉพาะออเดอร์ที่จ่ายแล้ว
- ราคาไทยถูกกว่าหน้าอังกฤษมาก — แยกสโตร์ **ห้ามบอกราคาบาทบนหน้าอังกฤษ**

---

## อ้างอิงในโปรเจกต์

- ไลเซนต์: `license_core.py`, `scripts/gen_license.py`
- ทดลองปัจจุบัน: `demo/uploads/demo-form.pdf` + `can_fill_document()`
- เทมเพลตโรงเรียน (ไม่ใช่โหมดฟรี): `formpacks/v1/`
- เว็บ: `website/`
- งานค้างที่ผูกกับแผนนี้: A3, A4, A6, B2, C1, C2, C5 ใน [BACKLOG.md](BACKLOG.md)
