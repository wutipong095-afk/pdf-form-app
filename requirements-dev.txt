# ใช้ตอนรันเทส / CI — ไม่ใส่ใน Docker หรือชุดติดตั้งลูกค้า
-r requirements.txt
pytest>=8.0
stripe==15.6.1

# Legacy fixtures and independent coordinate checks only; excluded from installer
PyMuPDF>=1.24
