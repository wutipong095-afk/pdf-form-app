# Releases

วางไฟล์จริงที่นี่บน VPS:

- `PDFFormMarker-Setup-x.y.z.exe`
- `latest.json` — คัดลอกจาก `latest.example.json` แล้วแก้ `version` / `setup_url`

ปุ่มดาวน์โหลดบนเว็บอ่าน `latest.json` ถ้ามี ไม่งั้นชี้ `PDFFormMarker-Setup-0.3.0.exe`
