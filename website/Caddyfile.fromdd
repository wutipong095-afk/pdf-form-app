# Caddy example — fromdd.xambrain.com
# วางเป็นไซต์แยก อย่าทับ config ของ shed.xambrain.com
#
# /var/www/fromdd ใส่ได้เฉพาะของสาธารณะ:
#   HTML / CSS / JS / รูป / FromDD-Setup-x.y.z.exe / latest.json / SHA256SUMS.txt
# ห้ามวางในโฟลเดอร์นี้:
#   private key, license-master.key, gen_license.py, .env, ฐานข้อมูลลูกค้า
# คีย์ออกไลเซนต์อยู่ที่ backend (env/secret) เช่น api.fromdd.xambrain.com ในอนาคต

fromdd.xambrain.com {
	root * /var/www/fromdd
	file_server
	encode gzip
	header {
		X-Content-Type-Options nosniff
		Referrer-Policy strict-origin-when-cross-origin
	}

	# แอปและเว็บใช้ไฟล์นี้ตรวจอัปเดต — ห้ามแคชนาน
	@latest path /releases/latest.json
	header @latest Cache-Control "no-cache"

	# ไฟล์ตามเวอร์ชันเป็น immutable — ห้ามทับ FromDD-Setup-x.y.z.exe เดิม
	@setup path /releases/*.exe
	header @setup Cache-Control "public, max-age=31536000, immutable"
}
